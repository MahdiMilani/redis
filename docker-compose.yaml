version: '3.8'
services:
  redis:
    image: redis
    container_name: redis_api
    ports:
      - "6379:6379"
    networks:
      - app-network
  app:
    build: .
    depends_on:
      - redis
    ports:
      - "8090:8090"
    networks:
      - app-network
networks:
  app-network:
